services:
  - type: web
    plan: free
    name: chuty_girl-md_v19
    runtime: docker
    region: Frankfurt
    branch: V-3.00
    repo: https://g
    #buildCommand: |
    #  apt-get update && \
    #  apt-get install -y ffmpeg && \
    #  npm install
    autoDeploy: true
    healthCheckPath: /
    #startCommand: npm start
    envVars:
      - key: SESSION_ID
        value: "YOUR_SESSION_ID"
